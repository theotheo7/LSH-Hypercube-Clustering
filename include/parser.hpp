#ifndef PARSER_HPP
#define PARSER_HPP

#include <iostream>
#include <fstream>
#include <string>

class Parser {
private:

public:

    Parser();
    ~Parser();

    void readInputFile(std::string);
};

#endif